/* Placeholder — copy sync-config.example.js values here to enable cloud sync.
   Until real credentials are set, the app stays fully offline (sync is a no-op). */
window.CC_SUPABASE_URL      = 'TU_PROJECT_URL';
window.CC_SUPABASE_ANON_KEY = 'TU_ANON_KEY';
